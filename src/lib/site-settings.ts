/**
 * Barrel: defaults + types cho site settings — an toàn cho client bundle.
 * Server-side `getSettings()` (đọc từ Postgres) nằm ở `@/lib/site-settings-server`.
 */
export * from "./site-settings-public";
