# SEO baseline — KAHA.VN

Theo skill `seo-baseline-preserve`: crawl CSV đầy đủ + GSC export lưu cùng thư mục này.

Có sẵn: snapshot home JSON, danh sách leaf sitemap, stub slug map, URL list (~1107), **`kaha-seed-crawl-20260423.csv`** (cột theo skill — chỉ URL điền, còn lại trống cho crawl/merge). App: `/sitemap.xml` + `/robots.txt` (đọc URL list; `NEXT_PUBLIC_SITE_URL` trong `.env.example`). Full crawl + GSC vẫn cần thêm.
