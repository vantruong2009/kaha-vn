# SEO baseline — KAHA.VN

Theo skill `seo-baseline-preserve`: crawl CSV đầy đủ + GSC export lưu cùng thư mục này.

Có sẵn: snapshot home JSON, danh sách leaf sitemap, stub slug map, **`kaha-urls-from-sitemaps-20260423.txt`** (~1107 URL, từ `scripts/seo_urls_from_sitemaps.sh`). Full crawl CSV cột Yoast/GSC vẫn cần thêm.
