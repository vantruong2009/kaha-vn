# KAHA.VN — state (handoff)

## Tiến độ (ước lượng)

| Phạm vi | % |
|--------|---|
| **Đến live + cutover (P0–P6, không tính P7 monitoring)** | **~88%** |
| P0 bootstrap + SEO prep | ~82% |
| P1 WP export + R2 | ~32% |
| P2 Obsidian đủ catalog/UI | ~58% |
| P3 tính năng lõi (variants, quote, …) | ~78% |
| P4 import DB + redirect map | ~74% |
| P5 perf budgets | ~52% |
| P6 staging + cutover | ~58% |

P7 (30 ngày giám sát) không tính vào % “tới live”.

- **Fork plan (2026-04-24):** user confirm KAHA kế thừa ĐẦY ĐỦ e-commerce của longdenviet.com (cart/checkout/auth/admin/payment) + luxury overlay Obsidian. Branch `archive/pre-fork-v1` + `redesign/fork-longdenviet` đã push GitHub. Kế hoạch 4-phase, chia nhiều session chat nhỏ: xem `docs/FORK-PLAN.md`. Source longdenviet: `/home/opc/cursor-workspace/projects/longdenviet/`. Session tiếp: Phase 1a — deps + lib utilities.
- **Phase:** P1 → P2 — Docker `standalone` + compose **`network_mode: host`**, bind `127.0.0.1:3102`, `env_file: .env.local`, `/api/health` **`db: ok`**. **SEO sitemap baseline:** `npm run seo:slug-diff` → **missing 0** (wildcard redirect `danh-muc`/`key`/`p` + script khớp `:path*`); slug archive `product_cat`/`product_tag` (vd. `/gia-cong-den-trang-tri`) → **`/shop?category=`** / **`?tag=`** (308). **R2:** rewrite + `normalizeMediaBase` env; ảnh qua `/_next/image`. **Logo:** `public/images/logo.png` (WP live). Còn: GSC, mockup homepage, DNS. **Số liệu tồn đọng:** `docs/audit/TODO-SNAPSHOT-2026-04-24.md`.
- **Stack:** Next 16 / React 19 / Tailwind 4; `loading`/`error`/`not-found` Obsidian; skip link `#main-content`; `/feed.xml` (RSS + plain excerpt), `/manifest.webmanifest`, phân trang `/journal` + **ItemList** JSON-LD; trang **`/shop`** (search/filter/pagination/sort), **`/lookbook`** (shop-the-look hotspots), **quote request** (`/api/quote`), **showroom booking** (`/showroom`, `/api/showroom-booking`), **moodboard localStorage** (`/moodboard`), **spec sheet** (`/spec/[slug]` in/lưu PDF); anti-spam leads (honeypot + rate-limit nhẹ); import XML chuẩn hóa excerpt/meta plain text; SQL leads `scripts/sql/003_leads_tables.sql`; one-shot pipeline `npm run migrate:run-all` / `migrate:run-all:dry`; SEO slug diff + redirect candidate merge (`npm run seo:slug-diff`, `npm run seo:merge-redirect-candidates`); `docs/ROLLBACK.md`; `docs/PRELAUNCH-CHECKLIST.md`; `docs/STAGING-RUNBOOK.md`; `npm run prelaunch:check` + `npm run perf:budget` + `npm run staging:smoke`; security headers + CSP trong `next.config`.
- **Build Oracle:** `node >=20.9` (tránh `/usr/bin/node` v16 — làm optional `@tailwindcss/oxide-*` fail). **`npm run dev` / `build` / `lint` / `migrate:*`** gọi `scripts/with-node20.sh` (ưu tiên Node trong `~/.cursor-server/.../linux-arm64/*/node`, hoặc `KAHA_NODE20=/path/node`). `npm install` vẫn có thể cảnh báo EBADENGINE nếu npm dùng Node 16 — có thể bỏ qua nếu đã cài đủ `node_modules`.
- **Safety:** §1c master plan — không đụng site live / ERP; chỉ boundary KAHA.
- **Next (thứ tự):** (1) **R2 bucket tách riêng:** đã chuyển config dự án sang bucket `kahavn` (env/runbook dùng `NEXT_PUBLIC_MEDIA_BASE=https://pub-...r2.dev`, sync target `s3://kahavn/`). Còn thao tác tay: bật Public Access bucket `kahavn` trên Cloudflare và rebuild staging để xác nhận ảnh đang đọc từ bucket mới. (2) **Nội dung:** product còn ~161 slug thiếu `body_html` sau fallback excerpt; post/page xem `docs/audit/TODO-SNAPSHOT-2026-04-24.md` (chưa query lại được trong phiên này do DB localhost không mở cổng `5432`). (3) **GSC:** đã thêm hỗ trợ token meta (`NEXT_PUBLIC_GSC_VERIFICATION`) + checklist `docs/GSC-CHECKLIST.md`; còn thao tác tay trong tài khoản Google. (4) **DNS cutover.** **Đã xong (2026-04-25):** Homepage `page.tsx` — thay toàn bộ `ImgPlaceholder` bằng `next/image` thật từ catalog DB: hero mosaic 3 ảnh, showcase 4 cards, spaces 4 cards, B2B 1 ảnh, blog thumbnails — qua `getHomepageImages()` + `rewriteKahaMediaUrls` (R2 CDN). TSC pass. **Đã xong thêm (2026-04-25):** fix mất shell toàn site — chuẩn hóa route `/` và route group `(main)` dùng shell đồng nhất (`SiteHeader`/`SiteFooter`), menu hiển thị lại, font global đồng bộ `Inter` (body/form controls) + `Playfair` (h1-h4); batch rebrand kỹ thuật LongDenViet → KAHA trên các route/chuỗi SEO chính. **Tiếp (2026-04-25):** blog Postgres qua `content_nodes` (`getPosts`, `commerce.ts`); `next.config` thêm `cdn.kaha.vn` cho `next/image`. **Đã xong trước đó:** import **933** node (477 product); redirect map + slug diff; logo file trong repo; importer Woo **body_html** từ excerpt khi WXR thiếu `content:encoded`; `lint` / `prelaunch:check` / `staging:smoke` / `perf:budget` PASS.
